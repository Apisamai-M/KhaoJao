using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class propoties : MonoBehaviour
{
    public int Life, TimeRevharge, cash;
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        //CheckDeath();
    }

   /* void CheckDeath()
    {
        if (life <= 0)
        {
            Destroy(gameobject);
        }
    }*/
        
}
